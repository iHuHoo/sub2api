# Aox upstream v0.2.0 review

Date: 2026-09-03

## Provenance

- Fork base: `iHuHoo/sub2api` `main` at merge commit `d199065206aedbbb08f4d5cd46690c1d2a9235c5`, which includes the first-stage `v0.1.185` synchronization.
- Official release: `Wei-Shaw/sub2api` `v0.2.0`, published 2026-09-02.
- Official annotated tag object: `dd07c4d8d484878e617c945cc8bacc304a5a6560`.
- Official peeled commit: `aa236488351eb71e120fc2b6fb32e36b0374c918`.
- The release metadata, tag object, and peeled commit match fresh GitHub and SSH checks against the official repository.
- The annotated tag is not cryptographically signed.

## Merge

- Strategy: non-squashed merge of the official tag into the Aox fork.
- Merge commit: `46b6c06e900dcee943034ae44c1550f40fa5e546`.
- Result: 60 upstream commits and 148 affected files relative to `v0.1.185`.
- Text conflicts: none.
- Of the 148 upstream-affected paths, 141 match the official tag byte-for-byte. The remaining seven intentionally retain Aox subscription promo-code additions in shared Ent-generated, domain, DTO, and frontend type files.

## Upstream changes

- Adds OpenAI Fast group policy, optional Standard-price billing for free Fast requests, and model-scoped reasoning-effort mapping with deny-or-downgrade ceilings.
- Adds native Kimi Responses API forwarding, Claude Fable 5.1 support, and scheduled automation bootstrap without a call ID.
- Fixes OpenAI WebSocket terminal-close handling, API-key chat-cache identity, scheduler passthrough projection, per-model cooldown error mapping, and Anthropic fallback filtering.
- Adds four SQL migration files: channel one-hour cache-write pricing and three group-policy columns under migration numbers 232 and 233.

## Aox compatibility review

- The Aox subscription promo-code schema remains migration 231; upstream migrations begin at 232, so there is no migration-number collision.
- The seven shared paths correctly combine upstream group fields with Aox promo-code fields. Ent regeneration changes only an upstream-generated comment and produces no executable-code delta, confirming the merged schema output is consistent.
- Aox subscription promo-code reservation, settlement, release, refund, one-use, non-stacking, and non-zero-order paths remain present.
- Aox CNY conversion, WooshPay, payment-provider snapshots, order expiry, and affiliate rebate logic remain present. Subscription affiliate rebates still use the order's discounted USD `Amount`, excluding gateway fees and gateway-currency display values.
- Aox build type, independent Aox versioning, release workflow, managed-update behavior, and upstream monitor remain present.
- The official `backend/cmd/server/VERSION` is `0.1.185`; release builds inject their tag version, while Aox builds continue to inject the independent Aox version and `BUILD_TYPE=aox`.

## Verification

- Pre-merge baseline: Go unit tests passed; frontend 252 files / 1,821 tests passed; frontend typecheck passed.
- Post-merge Go unit and integration suites passed, including the PostgreSQL/Redis integration path and migration tests.
- Post-merge frontend suite passed: 254 files / 1,842 tests. Lint, typecheck, and production build passed.
- `go vet ./...`, `go build ./...`, and `golangci-lint v2.13.1` passed with 0 issues under Go 1.27.0.
- Deployment shell syntax, Apple container lifecycle, Compose security/gateway/resources, Caddy cache, and upstream monitor tests passed.
- `git diff --check` passed.

## Release policy

This synchronization updates code only. It does not create an Aox tag, publish an image, or deploy production.
