# Aox upstream v0.2.8 review

Date: 2026-09-24

## Provenance and merge

- Fork base: origin/main at 797f3dea3ab814a7520809321ca167a17debf414.
- Upstream annotated tag: d7a82d78ca51d42be41cb4daa3510ea401defe9f.
- Peeled commit: fd80b08c90b55edcad5b00171b53f08721d30da1.
- Stable tag merged with history: 237 upstream commits since v0.2.7.
- Updated .github/upstream-baseline.json to the tag and peeled commit.

## Compatibility review

- Resolve HTTP/2 transport/test conflicts by retaining Aox's native Go 1.27 `http.HTTP2Config`, while adopting upstream OpenAI 15s/15s and long-stream 10s/5s idle/ping policies.
- Preserve upstream response cancellation and body-close changes. Adapt the cleartext HTTP/2 ping test to initialize its test-only `http.Protocols` explicitly; native transport configuration leaves that field nil.
- Preserve promo DTO fields and dependency injection, WooshPay, currency snapshots/conversion, promo lifecycle, discounted payment affiliate accounting, and managed release/deployment behavior.
- New affiliate offline withdrawal operates on affiliate quota and uses transactional idempotency; it does not replace Aox's discounted-payment rebate base.
- Reasoning-effort multipliers are now explicit maps. Unconfigured efforts use 1x; upstream removes the previous implicit Fable max multiplier.
- Independent read-only review found no concrete merge blockers; runtime checks are recorded in the PR.

## Migrations and rollback

- `238b_content_moderation_engine_meta.sql`: adds nullable audit provenance JSONB; earlier rows and application writes remain valid.
- `239_channel_reasoning_effort_multipliers.sql`: introduces pricing maps, copies explicitly configured channel max multipliers, and converts group pricing JSON while removing consumed legacy keys. Existing maps, including empty maps, win.
- `240_affiliate_ledger_operation_id.sql`: adds a nullable operation ID with a partial unique index for idempotent withdrawals.
- Before any application deployment, take and verify a database backup. A binary-only rollback cannot restore the group pricing keys consumed by migration 239, and subsequent new-map edits are not mirrored into the legacy channel column. Review/reverse pricing data explicitly or restore an appropriate backup with a separate data-loss assessment.

## Production infrastructure maintenance

- Updated host packages, rebooted into kernel 7.0.0-1014-azure, and verified Docker 29.8.1.
- Upgraded PostgreSQL 18.4 to 18.6 and Redis 8.8.0 to 8.10.2, pinned to immutable image digests.
- Verified logical dump catalog, cold data-directory archive and SHA-256 checksums. Backup location and deployed digests are in docs/PRODUCTION_DEPLOYMENT.md.
- All containers healthy, zero restart counts after recreation; public health/login return 200 and unauthenticated models return 401.
- Production application remains v0.2.7-aox.0.0.14. None of the v0.2.8 migrations have run there.

## Verification

- Backend: `go test -tags=unit ./...` and `go test -tags=integration ./...` pass; targeted HTTP/2 protocol and ping timing tests pass.
- golangci-lint 2.13.1: 0 issues. CGO-disabled backend build passes.
- Frontend lint/typecheck pass; all 334 Vitest files / 2,501 tests pass. Vue TypeScript and Vite production build pass (existing large-chunk warnings only).
- Deployment lifecycle, Compose security/gateway/resources and Caddy cache checks pass.
- Local Go: 1.27.1; CI remains pinned to 1.27.0. Local pnpm 9.15.9 invoked through Corepack.

## Release policy

Code synchronization only. No Aox release tag, application image publication, or production application deployment is included.
