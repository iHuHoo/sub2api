# Upstream v0.1.184 Aox review

## Scope

- Aox base: `iHuHoo/sub2api` `origin/main` at `faa2b57208e3e043cac511471ef340e18688f268`
- Previous upstream baseline: `v0.1.183` at `e8cb019fabf8b55199436229044cbf9aa7a82564`
- Official target: `v0.1.184` at `e98ef32eb29aecd30d1def615912ec4dc93173f3`
- Upstream delta: 170 commits and 342 changed paths

The annotated tag is not signed. Its tag object and peeled commit were verified against the official `Wei-Shaw/sub2api` SSH remote before merging. The merge keeps the upstream commit graph and is not squashed.

## Aox overlap audit

The three-way comparison found 18 paths changed by both upstream and Aox since `v0.1.183`.

| Area | Paths | Result |
|---|---|---|
| Generated backend | `backend/cmd/server/wire_gen.go`, `backend/ent/migrate/schema.go`, `backend/ent/mutation.go`, `backend/ent/runtime/runtime.go` | Combined. Ent and Wire regeneration produced no diff. |
| DTO/domain | `backend/internal/handler/dto/mappers.go`, `backend/internal/handler/dto/types.go`, `backend/internal/service/domain_constants.go` | Combined. Upstream usage fields and Aox payment/promo fields remain present. |
| Update policy | `backend/internal/service/update_service.go` | Combined. Upstream prerelease parsing is accepted; Aox version handling and managed-updater restrictions remain. |
| Admin translations | six `frontend/src/i18n/locales/{en,zh}/admin/{resources,settings}.ts` and `{en,zh}/misc.ts` files | Combined. Upstream TTFT/account strings and Aox payment/provider/promo strings remain. |
| Frontend types/settings | `frontend/src/types/index.ts`, `frontend/src/views/admin/SettingsView.vue` | Combined. Upstream settings and Aox provider/payment settings remain. |
| Payment UI | `frontend/src/views/user/PaymentView.vue`, `frontend/src/views/user/__tests__/PaymentView.spec.ts` | Combined. Upstream selected-currency recharge text is accepted; Aox subscription CNY conversion and subscription promo flow remain. The test file was the only textual conflict and retains both the Aox device mock and upstream translation mock. |

Payment, refund, affiliate USD accounting, daily-limit USD accounting, WooshPay, subscription promo reservation/consume/release, OAuth resume, Go 1.27.0, Aox build metadata, and the upstream monitor were checked as fork-owned invariants.

## Migration review

Upstream adds:

- `231_add_usage_log_native_compaction_v2.sql`
- `231_add_usage_log_requested_reasoning_effort.sql`
- `231_user_restrict_public_groups.sql`

Aox already has `231_subscription_promo_codes.sql`. The migration runner keys migrations by complete filename and sorts lexically, so the repeated numeric prefix does not collide. All four files are independent and idempotent. The integration suite applied the complete migration set to PostgreSQL 18.1, reapplied it, and passed schema checks.

## Regression finding

The official tag's `openAIFastPolicyRepoStub.GetMultiple` still panicked, while the new TTFT path legitimately calls the repository batch-read contract. The existing service-tier streaming test reproduced the panic. The test stub now returns requested values like the real repository; no production path was changed.

## Verification

- `make test-unit`: passed.
- `make test-integration`: passed, including testcontainers PostgreSQL 18.1 and Redis.
- High-risk backend payment/promo/billing/OAuth/usage test selection: passed.
- Full frontend Vitest: 252 files, 1821 tests passed.
- Frontend CI critical set: 13 files, 173 tests passed.
- Frontend lint, typecheck, and production build: passed.
- `go vet ./...`: passed.
- `golangci-lint v2.13.1`: 0 issues.
- Ent and Wire regeneration: clean.
- Deployment shell, Apple container lifecycle, Compose security/environment/resources, and Caddy cache tests: passed.
- Upstream monitor unit tests: 3 passed.

This synchronization does not create an Aox tag, publish an image, or deploy production.
