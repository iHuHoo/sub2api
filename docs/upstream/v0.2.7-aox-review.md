# Aox upstream v0.2.7 review

Date: 2026-09-20

## Provenance and merge

- Fork base: origin/main at e2c439901 (PR #27, v0.2.5).
- Upstream annotated tag: 7484192016807acf55c6ef4f2827d371eb61ec1c.
- Peeled commit: aea725f2ea644d5592d0bbb1d63b607efa7e200a.
- Fetched tag and GitHub API agree; upstream tag is unsigned.
- Merge the stable tag, retaining its history; 71 upstream commits since the recorded baseline.
- Updated .github/upstream-baseline.json to the tag and peeled commit.
- The upstream tag still contains VERSION 0.2.5; retained verbatim, as in prior syncs. Aox release builds supply their version explicitly.

## Compatibility review

- AmountInput.spec.ts add/add conflict: retain upstream invalid-input, decimal and clearing regressions plus Aox currency/quick-amount coverage.
- ChannelMonitorView.grok.spec.ts conflict: retain PROVIDERS.length rather than a hard-coded provider count.
- Preserve Aox subscription promo-code DTO fields and payment-service promo dependency injection.
- Preserve WooshPay, currency snapshots/conversion, promo reservation/consumption/release, discounted affiliate accounting and managed update/release behavior.
- Keep existing gRPC 1.83.2 dependency upgrade; upstream requires the same security fix.
- Reviewed redemption history handler/service/repository/API/UI pagination, stable sorting, payment config concurrency, refund amount warning, subscription loading reset and group usage rollup query changes.
- No new database migrations.
- Independent read-only review found no concrete merge regressions or blockers.

## Verification

Validation results are recorded in the synchronization PR.
Local Go is 1.27.1; CI retains pinned Go 1.27.0.
Deployment lifecycle checks use the existing mocked test harness.

## Release policy

Code synchronization only; no Aox tag, image publication or deployment.
Refs #28. The upstream monitor closes the tracking issue after the baseline reaches main.
