# Aox upstream v0.2.1 review

Date: 2026-09-07

## Provenance

- Fork base: `iHuHoo/sub2api` `main` at `916c6d2c20c678927ced6fdface33d836b0897eb`.
- Official release: `Wei-Shaw/sub2api` `v0.2.1`, published 2026-09-05.
- Official annotated tag object: `adc26f68f687685e847bfb997559f48e79cac475`.
- Official peeled commit: `578785ee7fb35030b094b69624efe25670a36f5f`.
- Fresh GitHub release metadata and SSH `ls-remote` results match the fetched tag.
- The annotated tag is not cryptographically signed.

## Merge

- Strategy: non-squashed merge of the official tag into the Aox fork.
- Merge commit: `8e8512f2f9360336da75be2f7505a14729002fff`.
- Result: 82 upstream commits and 297 affected paths relative to `v0.2.0`.
- Text conflicts: none.
- Of the 297 upstream-affected paths, 282 match the official tag byte-for-byte. The remaining 15 shared paths intentionally combine upstream changes with Aox subscription promo-code fields and release behavior, payment UI behavior, Go 1.27 native HTTP/2 keepalive, and existing Aox admin translations/settings.

## Upstream changes

- Adds GPT-6 Astra, Codex Ultrafast, pinned-account Codex model manifests, configurable Claude CLI version, image URL-to-`b64_json` backfill, and upstream request-ID recording.
- Adds Anthropic reasoning-effort pricing controls and pricing-file hot reload.
- Fixes gateway account/model selection, channel restriction checks, WebSocket replay memory and cyber-policy recording, OpenCode and continuation handling, Gemini native custom model lists, and delayed Alipay reconciliation.
- Adds migration files 232 through 234 for upstream request IDs, channel reasoning-effort pricing, and group Codex model-manifest configuration.

## Aox compatibility review

- Aox subscription promo-code migration 231 remains intact; the new upstream migrations start at 232, so there is no migration-number collision.
- Promo reservation, consumption, release, one-use enforcement, discounted subscription totals, and refund behavior remain present in the merged Ent schema and payment services.
- The upstream pending-payment reconciler now covers Alipay as well as WeChat. The Aox final promo-reservation release sweep remains immediately after expiry processing.
- WooshPay, subscription USD-to-CNY conversion, currency snapshots, and amount-precision paths remain unchanged.
- Affiliate rebates still use the discounted order USD amount for subscriptions rather than the CNY gateway payment amount.
- Go remains pinned to 1.27.0 in `go.mod` and both Dockerfiles. The Aox build type, independent Aox revision, managed updater, build workflow, and upstream monitor remain intact.
- The shared HTTP transport accepts upstream request-ID and attribution work while retaining the Go 1.27 native HTTP/2 PING configuration.

## Verification

- Pre-merge baseline: Go full suite passed; frontend 254 files / 1,842 tests passed.
- Post-merge backend unit and integration suites passed, including migration and Aox payment/promo regressions.
- Post-merge frontend lint, typecheck, and 257 files / 1,874 tests passed.
- Go vet, backend build, golangci-lint v2.13.1 (`0 issues`), and the frontend production build passed.
- Deployment-shell, Docker Compose security/gateway/resource, Caddy cache, Apple-container lifecycle, and upstream-monitor checks passed.

## Release policy

- The synchronization PR only updates code and the upstream baseline.
- An Aox release is requested after merge because this upstream version contains production-relevant payment reconciliation, gateway stability, security-hardening, and model compatibility fixes.
- Production deployment is explicitly deferred until the operator's later deployment window.
