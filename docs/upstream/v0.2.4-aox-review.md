# Aox upstream v0.2.4 review

Date: 2026-09-10

## Provenance

- Fork base: `iHuHoo/sub2api` `main` at `6ff41be2cb35a7009b250a7a59ebb7f5f3b5150c`.
- Official release: `Wei-Shaw/sub2api` `v0.2.4`, published 2026-09-09.
- Official annotated tag object: `d681d0798064ee0ffff376d19687d12f09fe600f`.
- Official peeled commit: `5de5e2bed035d43591a2e10e51f420ef6a84eb98`.
- Fresh GitHub metadata and `ls-remote` results match the fetched tag.
- The annotated tag is not cryptographically signed.

## Merge

- Strategy: non-squashed merge of the official `v0.2.4` tag into the Aox fork.
- Merge commit: `32070b4d65a8b8cf2fc2e11870946021c1f2d4fd`.
- Result: 266 affected paths relative to `v0.2.3`; 252 match the official tag byte-for-byte after the compatibility resolutions below.
- Text conflicts: `backend/internal/repository/http_upstream.go`, `backend/internal/repository/http_upstream_http2_keepalive_test.go`, and `frontend/src/views/user/__tests__/PaymentView.spec.ts`.
- Fourteen affected paths intentionally differ to preserve existing Aox build, generated schema, HTTP/2, settings, payment, and frontend type customizations.

## Upstream changes

- Adds complete MiniMax platform support, OpenAI Image 2.5, Grok media eligibility controls, and HTTP/2 PING keepalive for long-running streams.
- Adds estimated OpenAI weekly cost, user-ranking visibility control, Markdown payment help, draggable custom-page buttons, and Apple-container subnet support.
- Bounds system-log queries, invalidates cluster caches after configuration changes, persists account cooldown state, and improves stream-cancellation classification.
- Includes Antigravity plan recognition and other provider, proxy, monitor, UI, and dependency fixes.
- Adds migration 237 for the MiniMax platform.

## Aox compatibility review

- Aox subscription promo-code migration 231 remains intact; migration 237 does not collide with it.
- Promo reservation, one-use enforcement, discounted totals, WooshPay, payment-currency snapshots, and CNY presentation remain present and tested.
- Aox keeps the Go 1.27 native `http.Transport.HTTP2` configuration while adopting upstream's shared long-stream mode and 10-second/5-second PING timings.
- The payment-view tests retain Aox CNY/credits and same-tab WooshPay coverage while adding upstream's sanitized Markdown help coverage.
- The channel-monitor provider test now derives its expected button count from the canonical provider list so the new MiniMax provider remains covered.
- The Aox build type, independent release revision, managed updater, release workflow, and one-version-per-issue upstream monitor remain intact.

## Verification

- Pre-merge baseline: backend unit suite passed; frontend lint, typecheck, and 14 critical files / 176 tests passed.
- Post-merge backend unit and integration suites passed.
- Post-merge frontend lint, typecheck, 270 files / 1,987 tests, i18n validation, and production build passed.
- Go vet and backend build passed.
- Deployment-shell, Docker Compose security/gateway/resource, Caddy cache, and Apple-container lifecycle checks passed.
- PR CI remains the merge gate for the pinned golangci-lint and full repository workflow matrix.

## Release policy

- This synchronization updates code and the upstream baseline only.
- Aox artifact publication and production deployment remain separate operator decisions.
